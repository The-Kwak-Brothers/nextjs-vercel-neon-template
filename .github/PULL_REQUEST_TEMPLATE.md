## Summary

<!-- What changed and why -->

## Deploy target

- [ ] `DEPLOY_TARGET` intentional (`cloud` or `selfhosted`) — not mixed

## Test plan

- [ ] `nix develop -c npm run ci:check` (or `npm run ci:check`)
- [ ] Contract / relations checks if schema or API touched

## AI authorship

If an agent authored substantial parts of this PR, add a commit trailer:

`Co-authored-by: <Agent Name> <agent@example.com>`

AI-authored PRs still require **human review** before merge.
