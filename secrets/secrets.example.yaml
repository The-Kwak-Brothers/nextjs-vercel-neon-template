# Plaintext schema example only. Do not put real values in this file.
# `scripts/ci/bootstrap-sops.sh` encrypts this shape into secrets.enc.yaml.
common:
  APP_ENVIRONMENT: preview
cloud:
  EXAMPLE_APP_SECRET: replace-in-encrypted-file
selfhosted:
  EXAMPLE_APP_SECRET: replace-in-encrypted-file
