# Copy to secrets/.sops.yaml and replace the token with an age PUBLIC recipient.
# Never place an AGE-SECRET-KEY value in this file.
creation_rules:
  - path_regex: secrets\.enc\.yaml$
    age: <AGE_PUBLIC_RECIPIENT>
